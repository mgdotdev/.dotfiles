install() {
    if [ $2 = "deps" ]
    then
        sudo pacman -Sy stow
    else
        pushd ~/.dotfiles > /dev/null

        for folder in $(ls -d */ | sed "s/,/ /g")
        do
            stow -D $folder
            stow $folder
        done
        ~/.fzf/install
        popd > /dev/null
    fi
}

clean() {
    echo "nothing here yet..."
}

case $1 in
    "install") install $@ ;;
    "clean") clean $@ ;;
esac
